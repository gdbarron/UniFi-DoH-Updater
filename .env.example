# API key auth (recommended) - generate in UniFi OS: Settings > Integrations > API Keys
UNIFI_API_KEY=your_api_key_here

# OR username/password auth (uncomment if not using API key)
# UNIFI_PASS=your_password_here
